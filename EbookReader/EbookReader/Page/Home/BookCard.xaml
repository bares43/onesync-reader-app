<?xml version="1.0" encoding="UTF-8"?>
<StackLayout xmlns="http://xamarin.com/schemas/2014/forms" 
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="EbookReader.Page.Home.BookCard"
             WidthRequest="300"
             HeightRequest="500"
             BackgroundColor="Azure">
    <StackLayout.Children>
        <AbsoluteLayout VerticalOptions="FillAndExpand" HorizontalOptions="FillAndExpand">
            <AbsoluteLayout x:Name="Panel" BackgroundColor="Cyan" AbsoluteLayout.LayoutBounds="0,0,300,30">
                <Label Text="{Binding Percents, StringFormat='{0:N} %'}" />
            </AbsoluteLayout>
            <Label Text="{Binding Title}"
                   FontSize="Medium" 
                   BackgroundColor="#CC00FF00" 
                   TextColor="Wheat"
                   HorizontalTextAlignment="Center"
                   AbsoluteLayout.LayoutBounds="0, 430, 300, 70" />
        </AbsoluteLayout>
    </StackLayout.Children>
</StackLayout>